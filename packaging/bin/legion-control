#!/usr/bin/python3 -IB
import sys

sys.path.insert(0, "/usr/lib/legion-control")

if len(sys.argv) > 1:
    from legion_control.cli import main
else:
    from legion_control.ui import main

raise SystemExit(main())
