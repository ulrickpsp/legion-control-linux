.TH LEGION-CONTROL 1 "2026-08-12" "Legion Control 0.8.0" "User Commands"
.SH NAME
legion-control \- native controls for the supported Lenovo Legion 83LU
.SH SYNOPSIS
.B legion-control
.br
.B legion-control
.B status
.br
.B legion-control
.B doctor
.RB [ --json ]
.br
.B legion-control
.B scene
.IR silence | work | game
.br
.B legion-control
.B restore-firmware
.SH DESCRIPTION
.B legion-control
starts the unprivileged GTK4 interface. Hardware mutations are delegated to a
fixed PolicyKit helper. The current mutation allowlist contains only Lenovo DMI
product 83LU (Legion Pro 5 16IAX10H).
.PP
The application controls platform profiles, validated fan modes, bounded Custom
CPU power, selected device switches, and the exact ITE 048d:c195 24-zone RGB
keyboard endpoint. It installs no DKMS module.
.PP
The only network access is an opt-in release notice, disabled by default, which
asks the project's public releases listing once a day whether a newer version
exists and reports it on the Doctor page. It sends no identifier and downloads,
installs and executes nothing; installation remains the package manager's job.
.PP
.B status
and
.B doctor
are read-only terminal workflows. The latter emits a human-readable support
report, or JSON with
.BR --json .
Besides the hardware readings it checks the installation itself: the privileged
helper and its PolicyKit action, the Lenovo WMI kernel modules, the BIOS against
the validated one, the state of the fan service, and any other component that
writes
.I platform_profile
or drives the same ITE keyboard controller. Every problem carries the step that
resolves it. Nothing is written or probed on the hardware.
.B scene
applies a saved local scene and
.B restore-firmware
stops manual fan control. Both mutation commands use the same PolicyKit helper
and hardware allowlist as the GTK interface.
.SH SAFETY
Do not bypass product or controller checks. At 98 degrees Celsius, or when no
trustworthy temperature is available, the fan daemon returns control to
firmware. Package removal also attempts restoration.
.PP
If cooling is uncertain, stop heavy work and use the application's return-to-
firmware action before collecting diagnostics.
.SH FILES
.TP
.I /var/lib/legion-control/fan-config.json
Root-owned active fan policy.
.TP
.I /var/lib/legion-control/rgb-config.json
Last RGB configuration accepted by the helper; not semantic hardware readback.
.TP
.I ~/.config/legion-control/scenes.json
User-owned quick scenes.
.TP
.I ~/.config/legion-control/automation.json
User-owned opt-in AC/battery scene automation. It reacts only while the GTK
application is open.
.TP
.I ~/.local/state/legion-control/telemetry.jsonl
Bounded seven-day local telemetry archive. Set XDG_STATE_HOME to relocate it.
.SH SEE ALSO
.BR systemctl (1),
.BR pkexec (1)
.PP
Project documentation: https://github.com/ulrickpsp/legion-control-linux
.SH DISCLAIMER
Independent community software. Not affiliated with, endorsed by, or supported
by Lenovo.
